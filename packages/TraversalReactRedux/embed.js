import {
    createTheme,
    embedStart,
    embedStartButton
} from './dist/traversal-react-redux.cjs.production.min.js';

module.exports = {
    createTheme,
    start: embedStart,
    startButton: embedStartButton
}